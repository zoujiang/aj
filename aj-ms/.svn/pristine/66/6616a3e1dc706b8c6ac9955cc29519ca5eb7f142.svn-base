package com.frame.ifpr.common.author.impl;

import org.springframework.stereotype.Service;

import com.frame.ifpr.common.author.Author;
/**
 * 客户端认证的默认实现类
 * @author lishun
 *
 */
@Service
public class DefauleAuthor implements Author {

	@Override
	public boolean doAuthor(Integer userId) {
		// 目前不需要认证，所以所有认证都算通过
		return true;
	}

}
